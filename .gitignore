# Python and tool caches
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/
.DS_Store
*.png
*.pdf

# What the build makes for its own use: the full data files, the standalone page,
# the artifact copy and the workbooks. The served copies live in docs/. paths.py
# names every one of them.
/build/

# Private, never published: the research inputs the build reads, research
# scripts, fetch caches, the handoff and the archive. internal/START_HERE.md says
# what is where.
internal/

# Where build products and research inputs lived before Build 89. Kept, so a
# stray copy left at an old path stays private.
/ICON_Greater_Houston_Rolodex.html
/ICON_Greater_Houston_Rolodex.xlsx
/houston-data.json
/rolodex-artifact.html
/DFW_Rolodex.xlsx
/dfw/dfw-data.json
/dfw/pack/
/dfw/edit/
/dfw/hand.json
/dfw/FINDINGS_R1.md
/audit7/

# The intro reel's videos and frame sheets are rebuilt by hand, not committed.
intro/*.mp4
intro/_frames/
