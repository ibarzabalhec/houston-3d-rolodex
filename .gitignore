__pycache__/
*.pyc
*.png
*.pdf
# build products at the root; the served copies live in docs/
/ICON_Greater_Houston_Rolodex.html
/ICON_Greater_Houston_Rolodex.xlsx
/houston-data.json
/rolodex-artifact.html
# the internal register is tracked but must never be copied into docs/ output

# the internal research register stays local, out of the published repo
internal/

# Build 68: the Dallas-Fort Worth build product; its served copy lives in docs/
/dfw/dfw-data.json
/DFW_Rolodex.xlsx

# Build 71. The tool is external. Research notes stay on this machine: the Grok
# research pack, the DFW edit files and hand readings, the audit's edit files,
# and the build log and roadmap, which now live in internal/.
/dfw/pack/
/dfw/edit/
/dfw/hand.json
/dfw/FINDINGS_R1.md
/audit7/
# intro reel: videos and frame sheets are rebuilt by hand, not committed
intro/*.mp4
intro/_frames/

# tool caches
.ruff_cache/
.pytest_cache/
.DS_Store
